# Adicione essas variáveis ao seu .env.local

# API Keys para o app mobile (gere keys únicas para produção)
MOBILE_API_KEYS=mobile-dev-key-123,mobile-prod-key-456

# Domínio do app mobile (para CORS em produção)
MOBILE_APP_DOMAIN=https://yourmobileapp.com

# JWT Secret (se você decidir usar JWT)
JWT_SECRET=your-super-secret-jwt-key

# URL base da API para o mobile
API_BASE_URL=http://localhost:3001

# Em produção, seria algo como:
# API_BASE_URL=https://yourdomain.com
